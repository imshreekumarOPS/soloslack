High Impact / Relatively Easy

Note tags/labels — tag notes and filter by tag; pairs well with the search already in place
Card due date reminders — visual overdue indicators already exist; add a "Due Soon" dashboard section
Board templates — pre-built column layouts (e.g. "Bug Tracker", "Sprint Board", "Content Pipeline") to skip setup
Pin notes/boards — star/pin items so they always appear at the top of the dashboard
Note word count + reading time — small stat in the editor toolbar
Medium Effort / High Value

Markdown export — export notes as .md files (board export as JSON already exists)
Card checklists — subtasks inside a card with a completion progress bar on the card face
Column WIP limits — set a max card count per column, highlight when exceeded
Full-text search across everything — unified search bar (notes body + card titles/descriptions)
Keyboard shortcut cheatsheet — ? key opens a modal listing all shortcuts
Bigger Features

Note linking — [[Note Title]] wiki-style links between notes
Board calendar view — visualize cards with due dates on a monthly calendar
Archive — soft-delete notes/boards into an archive instead of permanent deletion
Drag-to-reorder boards — reorder boards in the sidebar via drag-and-drop
Focus mode — distraction-free full-screen note editor (hide sidebar)
